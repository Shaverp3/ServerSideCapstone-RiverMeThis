@model IEnumerable<RiverMeThis.Models.River>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalLength)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumAPs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MapPath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalLength)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumAPs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MapPath)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RiverId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RiverId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RiverId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
